def _rotate_fwht(self, x: torch.Tensor) -> torch.Tensor:
        """
        FWHT 旋转：x ⊙ sign → butterfly → ⊙ sign / √d

        x ⊙ sign → butterfly → ⊙ sign / √d
        = (H @ (x ⊙ sign)) ⊙ sign / √d
        = (x ⊙ sign) @ H ⊙ sign / √d（因为 H 对称）
        = x @ Q^T ✓
        """
        xr = x * self.signs
        self._fwht分层(xr)
        xr = xr * self.signs / self._sqrt_d
        return xr

    def _unrotate_fwht(self, y: torch.Tensor) -